[package]
name = "number_guessing_game"
version = "0.1.0"
edition = "2021"
description="A simple guessing game where a user plays against the computer. The user tries to guess a number between 1-100 that is generated by the computer randomly."
license = "MIT OR Apache-2.0"

[dependencies]
rand = "0.8.5"